apiVersion: v1
kind: Service
metadata:
  name: {{ include "teachers-api.fullname" . }}
  labels:
    {{- include "teachers-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "teachers-api.selectorLabels" . | nindent 4 }}